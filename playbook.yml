
---
- name: install wartest
  become: true
  become_user: jenkins
  hosts: 192.168.119.136
  vars:
    ansible_python_interpreter: /usr/bin/python3
  tasks:
    - name: Afficher un message
      command: echo "Installation du conteneur wartest en cours"

